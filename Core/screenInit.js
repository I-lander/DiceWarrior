let marginTop = 0;
let marginLeft = 0;

export function screenInit(canvas) {
  const screenWidth = innerWidth;
  const screenHeight = innerHeight;
  canvas.width = screenWidth;
  canvas.height = screenHeight
};
